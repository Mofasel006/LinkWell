# Convex deployment URL (obtained from `npx convex dev`)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Note: The OPENAI_API_KEY should be set in the Convex dashboard
# Go to your Convex project settings > Environment Variables
# and add OPENAI_API_KEY there (not in this file)
